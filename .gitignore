# .NET
**/bin/
**/obj/
.vs/
*.user
*.suo
TestResults/
coverage/
!deploy/ci/fixtures/coverage/
!deploy/ci/fixtures/coverage/**
ci-artifacts/
# Local scratch build/test output (W-0105 era tooling wrote 246 MB here). Never committed.
.artifacts/
# W-0193. Linked git worktrees live here. They are checkouts of this same repository, so adding
# one to the index would either commit a stray gitlink or, worse, a second copy of the tree.
.claude/worktrees/
# Generated reports, model downloads and rendered audio are local/CI output.
# Versioned pins, hashes, manifests and evidence metadata belong outside this directory.
/artifacts/
.tools/
deploy/lab/.local-tools/

# Node / Next.js
**/node_modules/
**/.next/
admin-ui/out/
admin-ui/.env*
!admin-ui/.env.example
npm-debug.log*

# Local runtime data and secrets
.env
.env.*
!.env.example
.secrets/
pgdata/

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Local assistant output and code index
.codex-doc-memory
/.codex-artifacts/
/.gitnexus/

# Python environments and caches
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
**/__pycache__/
*.py[cod]


# W-0251. The gate sweep lock is per-run state, never a tracked file.
deploy/ci/.gate-sweep.lock
