# W-0139 negative fixture: a secret name without a key must fail Helm rendering.
observability:
  enabled: true
  endpoint: https://otel-collector.telemetry.svc:4317
  protocol: grpc
  collector:
    namespaceLabels:
      kubernetes.io/metadata.name: telemetry
    podLabels:
      app.kubernetes.io/name: otel-collector
  headersSecret:
    existingSecret: platform-owned-otel-headers
