# REVIEW — Open Decisions Register

Trạng thái: `OPEN` · Cập nhật: `2026-09-25` — sửa theo chốt chief `25/09` ([danh sách chief](../../plan/toan-viec-can-lam-m8-2026-09-25.md)
mục `A1` bước 3; phiếu Sếp `25/09`, phần “Tech Lead và chief tự quyết”, dòng “Chốt 25/09 — M8”). Lượt ký
gốc: `2026-09-05` (`W-0194` — owner ký 19 dòng trong một lượt; xem
[gói ký OD-V1](../../plan/ivr-orther/00-DA-XONG.md#od-v1-signoff)). Không đóng bằng suy luận.

> Mock/fake fixture **không bao giờ** đóng một dòng nào trong bảng này. Mock chỉ cho phép code tiếp tục.

**Còn mở sau lượt sửa `2026-09-25`: mười hai dòng `OD-V1`**, đếm như `gate-status.mjs` (một dòng là mở khi ô
`Current` không có nhãn `CLOSED`). Năm dòng `OD-VOICE` đều đóng. Không dòng nào mở vì M8 thiếu ý kiến; mỗi
dòng chờ một bên cụ thể:

- **Module 3 đối ký** (`M8_POSITION_SIGNED / M3_NOT_RECEIVED`): `OD-V1-01`, `OD-V1-05`.
- **Tech Lead ở vai M3**, bản ký do chief chuyển: `OD-V1-02`, `OD-V1-03`, `OD-V1-06`. `OD-V1-07` chờ Tech Lead
  ký sau dòng ủy quyền `N14`.
- **Sếp**: `OD-V1-08`, `OD-V1-16` (phiếu Sếp `25/09` mục `N16`); `OD-V1-17`, `OD-V1-18` phụ thuộc phương án B
  (mục `B2`).
- **Số đo**: `OD-V1-09` (nửa sau: bảng ánh xạ tín hiệu nhà mạng → result, chỉ đo được khi có SIM thật) và
  `OD-V1-10` (dung lượng `32` eSIM: cần mô hình tải và thông lượng thật của một kênh).

Bản `2026-09-05` của đoạn này liệt kê bốn dòng mở (`OD-V1-09`, `OD-V1-10`, `OD-V1-11`, `OD-V1-21`) và đã cũ
từ `2026-09-10`, khi `OD-V1-11` và `OD-V1-21` đóng phần quyết định; ràng buộc về người duyệt thứ hai của hai
dòng đó vẫn còn, ghi ở từng dòng. Vế `S3` (giữ vĩnh viễn) của `OD-V1-11` có mở lại như `OD-V1-17`/`OD-V1-18`
hay không là câu hỏi đang gửi chief; tới khi có trả lời, dòng giữ nguyên trạng thái.

## P0 — real Sales integration/business acceptance

| ID | Decision/data | Owner | Current | Closure evidence |
| --- | --- | --- | --- | --- |
| `OD-V1-01` | program/payment/IVR-required/callable matrix | Sales Product/Core | ⏳ `M8_POSITION_SIGNED / M3_NOT_RECEIVED` 2026-09-05 — ký đúng ma trận runtime đang thực thi: `GOLDEN_HOUR+ONLINE` và `TWENTY_FOUR_SEVEN+COD`, cả hai cần `ivr_confirmation_required=true` và `order_state=CONFIRMING`; mọi tổ hợp khác từ chối fail-closed | signed matrix + producer tests |
| `OD-V1-02` | generic callback path and ACK taxonomy | Tech Lead (vai M3) *(sửa 25/09 theo chốt chief; trước ghi Sales API/Core)* | ⏳ `M8_POSITION_SIGNED / M3_NOT_RECEIVED` — **chờ bản ký Tech Lead do chief chuyển** *(sửa 25/09 theo chốt chief; hậu tố giữ vì ngày 25/09 anh Mạnh báo chưa nhận `IR-07` bản 17/09)*. M8 ký 2026-09-05 Target V1: `POST /api/v1/internal/orders/{orderId}/ivr-result-callbacks`; ACK 200 `ACCEPTED`/`DUPLICATE_ACCEPTED`/`BLOCKED_BY_CORE`/`REVIEW_REQUIRED`, 409 `REJECTED_STALE`/`IDEMPOTENCY_CONFLICT`. Endpoint Golden Hour giữ vai trò compatibility-only | OpenAPI + contract tests |
| `OD-V1-03` | order version exposure/bump/stale behavior | Tech Lead (vai M3) *(sửa 25/09 theo chốt chief; trước ghi Sales Core)* | ⏳ `M8_POSITION_SIGNED / M3_NOT_RECEIVED` — **chờ bản ký Tech Lead do chief chuyển** *(sửa 25/09 theo chốt chief; hậu tố giữ như `OD-V1-02`)*. Lập trường M8 ký 2026-09-05: Sales phơi `order_version`; IVR đính bản nó thấy; Sales revalidate và tự quyết. Lệch phiên bản trả ACK `REJECTED_STALE` và **không** ghi đè kết quả khách đã bấm | DTO + stale tests |
| `OD-V1-04` | speech-safe summary schema/content/item limits | Sales/Product/Privacy | ✅ `CLOSED` 2026-09-05 — ký schema `privacy_safe_order_summary` hiện hành; quy tắc gộp: quá 3 dòng hàng thì đọc 2 dòng đầu + "và N món khác", **tổng tiền không đổi** | schema + samples + approval |
| `OD-V1-05` | dial-token issue/resolve/TTL/one-use | Sales/Security/Telephony | ⏳ `M8_POSITION_SIGNED / M3_NOT_RECEIVED` 2026-09-05 — Sales cấp token lúc tạo task; TTL theo `OD-V1-17`; mỗi lần resolve ghi audit kèm `attempt_id`; resolve quá hạn hoặc quá số lần → từ chối và mở review, **không** im lặng bỏ qua | API/threat model/tests |
| `OD-V1-06` | no-answer/timeout/revalidation semantics | Tech Lead (vai M3) *(sửa 25/09 theo chốt chief; trước ghi Sales Product/Core)* | ⏳ `M8_POSITION_SIGNED / M3_NOT_RECEIVED` — **chờ bản ký Tech Lead do chief chuyển** *(sửa 25/09 theo chốt chief: dòng này từng ghi đóng ngày 2026-09-05 bằng chữ ký owner IVR một mình, trong khi quyết định thuộc vai M3)*. **Viết lại theo flow 04:** IVR **không bao giờ** tự hủy đơn. Sau `IVR_NO_ANSWER_FINAL`, Module 3 xử lý đơn ngay khi nhận callback, theo chương trình: đơn `TWENTY_FOUR_SEVEN` (COD) thì hệ bán hàng **hủy** với lý do `IVR_NO_ANSWER_MAX`; đơn `GOLDEN_HOUR` thì **xác nhận hết hiệu lực** và nhả suất theo flow 05 (lý do ghi `IVR_NO_ANSWER_MAX`), không hủy như đơn COD. Bỏ câu cũ “Core không đổi trạng thái, đơn tự hết hạn theo timeout”. Trên dây, `IVR_NO_ANSWER_FINAL` vẫn đi kèm `recommended_core_action = CORE_NO_STATE_CHANGE_WAIT_FOR_TIMEOUT`: giá trị này bị constraint `ck_ivr_call_results_action_matches_type` khoá và giữ nguyên (chief giữ constraint từ 16/09), nhưng Module 3 **không làm theo nó** — xem đính chính 25/09 cuối `IR-07`, dòng `A-10` | sequence + runtime tests |
| `OD-V1-07` | production auth and mTLS | Security/Platform | ⏳ `M8_POSITION_SIGNED` — **chờ Tech Lead ký, sau dòng ủy quyền `N14`** của phiếu Sếp 25/09 *(sửa 25/09 theo chốt chief: dòng này từng ghi đóng ngày 2026-09-05 bằng chữ ký owner IVR một mình)*. Lập trường M8: JWT ký khóa bất đối xứng, JWKS, TTL ≤ 10 phút, bắt buộc scope `ivr.task.write`. mTLS **hoãn** tới khi có hạ tầng thật; token dùng chung tĩnh bị từ chối dứt điểm khi provider là `TARGET_V1` | signed auth profile + tests |

> ### ⚠️ `W-0304` (16/09/2026) — bốn dòng trên đã đổi nhãn, và lý do đáng đọc
>
> `OD-V1-01`, `OD-V1-02`, `OD-V1-03` và `OD-V1-05` trước đây mang nhãn ✅ `CLOSED`. Cột **Owner**
> của cả bốn ghi *Sales Product/Core*, *Sales API/Core*, *Sales Core*, *Sales/Security/Telephony*
> — tức là **Module 3**. Nhưng chữ ký `2026-09-05` là chữ ký của **owner M8**, một mình.
>
> M3 **chưa nhận** bất kỳ bản nào trong số đó, chưa phản hồi, và chưa ký đối ứng. Gọi một
> quyết định song phương là `CLOSED` khi mới có một chữ ký là **sai về sự thật**, và cái giá
> của nó không trừu tượng: một bản rà soát đọc sổ này sẽ đếm bốn mục đã xong và kết luận
> phần tích hợp M3 đang tiến triển, trong khi thực tế nó **chưa bắt đầu** — đúng lỗi mà bản
> worklist `16/09` mắc phải khi chấm `%` cho các task phụ thuộc M3.
>
> Nhãn mới tách đúng hai việc vốn khác nhau:
>
> | | |
> | --- | --- |
> | `M8_POSITION_SIGNED` | M8 đã chốt lập trường của mình, đã implement, và **không định đổi** |
> | `M3_NOT_RECEIVED` | M3 chưa nhận, chưa phản hồi — nên chưa thể gọi là đã thống nhất |
>
> Đây **không** phải mở lại quyết định. Nội dung từng dòng giữ nguyên từng chữ, code không đổi
> một dòng, và M8 vẫn thi hành đúng như đang chạy. Chỉ có **nhãn trạng thái** trở nên đúng với
> thực tế. Khi M3 phản hồi, chuyển thẳng sang `CLOSED` mà không phải sửa gì khác.
>
> **Sửa `2026-09-25` (chốt chief):** người ký phía Module 3 của `OD-V1-02`, `OD-V1-03` và `OD-V1-06` là
> **Tech Lead ở vai M3**; chief chuyển bản ký. Hậu tố `M3_NOT_RECEIVED` giữ nguyên, vì ngày `25/09` anh
> Mạnh báo chưa nhận `IR-07` bản `17/09`; đổi sang `PENDING_M3_SIGNOFF` khi Module 3 xác nhận đã nhận.

## P0 — lab/production calls

Cột `Current` được thêm `2026-09-05` (`W-0194`). Trước đó bảng này chỉ có bốn cột, nên
`gate-status.mjs` đọc nhầm cột `Gate` làm trạng thái — một dòng đã đóng ở đây sẽ không bao giờ
được đếm là đóng. Bốn cột như các bảng khác là điều kiện để bảng điều khiển nói đúng.

| ID | Decision/data | Owner | Current | Gate |
| --- | --- | --- | --- | --- |
| `OD-V1-08` | final attempt policy/version, two-program bundle/hash, T0/counting/retry/quiet-hours/cutover | Product + Order Core + M3 | ⏳ `M8_POSITION_SIGNED` — **chờ Sếp** (phiếu Sếp 25/09 mục `N16`) *(sửa 25/09 theo chốt chief: dòng này từng ghi đóng ngày 2026-09-05 bằng chữ ký owner IVR một mình — chính `src/Ivr.Infrastructure/Persistence/Migrations/20260916065842_UnapproveUnsignedProductionAttemptPolicy.cs:20-23` (`W-0300`) ghi như vậy, và migration đó xoá hai dòng `gh-247-prod-v1` mà `W0196` đã chèn với `approved_for_production = TRUE`)*. Lập trường M8 — owner IVR ký bộ số `D-10` làm production version `gh-247-prod-v1`: GH `2/[0,150]/300s`, 24/7 `2/[0,450]/900s`; khung giờ gọi `08:00–21:00` ICT — **sửa `2026-09-07` (`W-0220`): owner chuyển `End` sang `21:08`** để đơn 24/7 nhận lúc `21:00` vẫn đủ hai cuộc; `21:07:30` owner nêu không biểu diễn được vì trường là phút và gate bỏ giây, `21:07` sẽ đóng đúng lúc cần mở; technical retry trần `1`, backoff `60s`; `T0` = lúc Order Core mở cửa sổ. Bộ phase-8 bị supersede vì tự mâu thuẫn (cửa sổ 600s cho một "Giờ Vàng" 5 phút) | production; triển khai ở GĐ 2 |
| `OD-V1-09` | 1 SIM lab protocol/DTMF/disposition/allowlist | Infra/vendor | ⏳ `HALF_SIGNED` 2026-09-05 — **đã ký**: giao thức `LAB-01..08`, allowlist đúng một alias đích, `REAL_CUSTOMER_CALL_ALLOWED=NO` suốt lab. **Chưa ký**: bảng ánh xạ tín hiệu nhà mạng → result; bảng hiện tại là ứng viên và chỉ đo được khi có SIM thật | LAB_REAL_SIM |
| `OD-V1-10` | 32 eSIM capacity/failover/caller-ID/cost | Infra/procurement | ⛔ `NOT_SIGNED` — cố ý. Con số 32 là giả định chứ chưa phải phép đo; ký bây giờ là ký một điều chưa biết. Mở lại sau bước 4.5 (mô hình tải) và 5.4 (thông lượng thật một kênh) | production |
| `OD-V1-11` | script/legal/do-not-call/retention | Legal/Privacy | ✅ `CLOSED` 2026-09-10 — **owner tuyên bố quorum là chính mình.** Tổ chức này không có đội Legal/Privacy và owner chọn **không** mua ý kiến pháp lý ngoài cho V1; owner nhận rủi ro pháp lý của nội dung đã ký bên dưới. **Đóng cái gì và KHÔNG đóng cái gì:** đóng **quyết định chính sách** (ghi âm, thời hạn lưu). **Không** đóng được ràng buộc kỹ thuật — `PRODUCTION_REAL` vẫn cần **ba actor id khác nhau** và code vẫn từ chối với một người, nên duyệt script cho production **vẫn chặn**, và đó là một sự thật vận hành riêng chứ không phải chữ ký còn thiếu. Muốn mở phải hoặc có người thứ hai/ba, hoặc ký một OD mới đổi chính luật ba-actor. **Sửa `2026-09-17` (`S3` của [bản vướng mắc `17/09`](../../plan/ivr-orther/vuong-mac-va-quyet-dinh-2026-09-17.md); ghi `18/09` ở `W-0316`): thời hạn lưu không còn như bản ký.** Owner quyết định IVR giữ **toàn bộ** dữ liệu, không đặt kỳ hạn xoá nào — kể cả nhật ký quản trị. Ba kỳ hạn trong nguyên văn dưới đây (metadata `90` ngày · bằng chứng DTMF `180` ngày · audit admin `1` năm) **bị thay**. Cách xoá duy nhất còn lại là khi khách yêu cầu (DSAR, `W-0314`); hiện chưa có lối chạy (`S8`). Rủi ro pháp lý của việc giữ vĩnh viễn là rủi ro `2` ở `S2`: **chờ Sếp ký (phiếu Sếp 25/09 mục `B2`)** *(sửa 25/09 theo chốt chief: câu cũ ghi Sếp đã ký nhận ở `S2`; thực tế phiếu `S2` do Toàn nhận có điều kiện ngày 22/09 (`W-0340`), ô ký của Sếp còn trống)*. Ghi âm vẫn TẮT, phần ba-actor ở trên không đổi. Nguyên văn trạng thái cũ: ⏳ `CONTENT_SIGNED / APPROVER_QUORUM_UNRESOLVED` 2026-09-05 — **đã ký**: ghi âm TẮT vĩnh viễn ở V1; metadata cuộc gọi 90 ngày; bằng chứng DTMF 180 ngày; audit admin 1 năm; `phone_ref`/dial token TTL ngắn nhất, xóa khi job đóng. **Chưa giải được bằng chữ ký**: `PRODUCTION_REAL` cần ba actor id khác nhau (`ScriptContentContracts.EnsureApprovalAllowed`: người tạo không được duyệt; `CONTENT` ≠ `PRIVACY_LEGAL`). Dự án hiện có một người | customer calls |
| `OD-V1-12` | pilot/release authority/kill switch | Release owner | ✅ `CLOSED` 2026-09-05 — owner là release authority. `REAL_CUSTOMER_CALL_ALLOWED` chỉ bật bằng quyết định ký tên có ngày kèm bằng chứng lab đạt và kịch bản đã duyệt; không bao giờ bằng biến môi trường hay một lượt deploy. **Bổ sung 25/09 (theo chốt chief; đề xuất của chief ở phiếu Sếp 25/09 mục `N04`):** lần bật `REAL_CUSTOMER_CALL_ALLOWED` **đầu tiên** cần **chữ ký Sếp**, vì gọi khách thật chạm dữ liệu cá nhân và pháp lý. Dev hay Tech Lead không tự ký một mình. Tới lúc đó giữ khoá `K1` (`REAL_CUSTOMER_CALL_ALLOWED=NO`) cho mọi nhóm | production |

## P0 — mở bởi red-team review 2026-08-12 (W-0062)

| ID | Decision/data | Owner | Current | Closure evidence | Gate |
| --- | --- | --- | --- | --- | --- |
| `OD-V1-13` | **Golden Hour ONLINE có thuộc scope IVR không.** Business source hiện đọc được là COD-only: `plan/ivr-orther/decisions-log.md` DS-01 (“IVR-callable = CHỈ `CONFIRMING` VÀ CHỈ khi `payment_method_snapshot=COD`”, source-read từ Sales platform). Target V1 §4 đề xuất thêm `GOLDEN_HOUR+ONLINE`. Delta này **chưa được owner phê duyệt**. | Product/Business + Sales Core | ✅ `CLOSED` 2026-09-05 — owner chọn **giữ cả hai chương trình**. Toàn bộ runtime/OpenAPI/DB/seed/test đã xây hai chương trình; rút về COD-only là xóa code đang chạy để khớp một lần đọc nguồn cũ hơn. **Kèm theo một việc thật chưa làm**: `DS-02` nói Core chỉ chuyển trạng thái cho đơn COD và từ chối `422` đơn non-COD, nên Sales phải định nghĩa transition cho `ONLINE` trước khi nối thật | Signed program matrix + Sales producer phát `GOLDEN_HOUR+ONLINE` task | real integration |
| `OD-V1-14` | **`ivr_confirmation_required` không có business source.** `grep -rln ivr_confirmation_required docs/documents/` → 0 hit. Cả OpenAPI (`enum:[true]`) và DB (`must be true`) đang gate trên một field chưa có nguồn business đã khóa. | Product/Business + Sales Core | ✅ `CLOSED` 2026-09-05 — giữ trường, định nghĩa dứt điểm: Order Core đặt `true` **chỉ cho** đơn nó muốn IVR gọi. Một cờ opt-in tường minh là thứ ngăn "đổi một câu query rồi vô tình gọi thêm ba nghìn đơn"; suy diễn ngầm không có phanh đó | Định nghĩa field + owner sign-off + producer test | real integration |
| `OD-V1-15` | **Speech variable whitelist.** Hai bộ spec active mâu thuẫn: bộ hẹp 4 biến (`specs/data/05-pii-policy.md`, `specs/ui/04-ivr-menu-config.md`, `specs/api/04-sim-adapter-contract.md`) vs bộ Target V1 cần thêm `items[]` (public_name, quantity) và `delivery_area_short` (`docs/contracts/target-v1-closure-pack/T-03-speech-summary.md`, governance §2.7). Business source `PACK-09 §9.1` hậu thuẫn bộ hẹp. **Mở rộng whitelist tự nó là một quyết định privacy.** | Product + Privacy/Legal | ✅ `CLOSED` 2026-09-05 — owner chọn **bộ rộng**: tên ngắn, mã đơn, `items[].public_name` + `quantity` (+ `unit_label`), tổng tiền, `delivery_area_short`. Lý do: một cuộc gọi xác nhận không nói đang xác nhận **cái gì** thì không phải xác nhận. Rủi ro đã cân: người nhấc máy không phải chủ đơn sẽ nghe được món hàng và phường/quận; đổi lại là khách phát hiện đơn sai trước khi hàng đi. Guard giữ nguyên: cấm địa chỉ đầy đủ, cấm chi tiết thanh toán, `delivery_area_short` không được bắt đầu bằng chữ số | Approved whitelist + PIA/privacy sign-off + cập nhật đồng bộ 3 spec | business acceptance |
| `OD-V1-16` | **Attempt policy delta vs business source.** Phase-8 ghi GH `2/[0,300]/600s`, 24/7 `3/[0,300,600]/900s`; D-10 và candidate `mock-lab-v1` ghi GH `2/[0,150]/300s`, 24/7 `2/[0,450]/900s`. W-0151 còn tìm thấy production governance gaps: row-by-row registry, thiếu signed refs/four-eyes/effective-retire/bundle atomicity; technical retry config chưa versioned; pre-dial flag không so job policy; quiet-hours/timezone chưa có contract. Current wire **đã** exact compare và trả `409`, không phải khoảng trống. | Product + Order Core + M3; Platform/M8/Release ở dòng kỹ thuật | ⏳ `M8_POSITION_SIGNED` — **chờ Sếp** (phiếu Sếp 25/09 mục `N16`) *(sửa 25/09 theo chốt chief: dòng này từng ghi đóng ngày 2026-09-05 bằng chữ ký owner IVR một mình, như `OD-V1-08`; xem migration `20260916065842_UnapproveUnsignedProductionAttemptPolicy.cs:20-23`)*. Lập trường M8 — owner supersede nguồn phase-8, lấy bộ `D-10`: GH `2/[0,150]/300s`, 24/7 `2/[0,450]/900s`, version production `gh-247-prod-v1`. Phần `D-10` không nói thì ghi thêm ở đây: khung giờ gọi `08:00–21:00` ICT — **sửa `2026-09-07` (`W-0220`): owner chuyển `End` sang `21:08`** để đơn 24/7 nhận lúc `21:00` vẫn đủ hai cuộc; `21:07:30` owner nêu không biểu diễn được vì trường là phút và gate bỏ giây, `21:07` sẽ đóng đúng lúc cần mở. Khung giờ này **đã có trong code** từ `W-0198`, nay mặc định `08:00–21:08` giờ Việt Nam (`src/Ivr.Infrastructure/Scheduling/CallingWindow.cs:37`, `:59`) *(sửa 25/09: câu cũ ở đây ghi khung giờ “hiện chưa có” và hệ thống sẵn sàng gọi lúc 3 giờ sáng nếu có task — hết đúng từ `W-0198`)*; technical retry trần `1` backoff `60s`. Giữ `mock-lab-v1` nguyên tên làm bản lab | Signed `ATP-01..15` + canonical two-program version/bundle hash + M3 producer SHA/OpenAPI/CDC/shared tests + owner business supersede nguồn cũ; xem [M8-11](../../plan/ivr-orther/m8-11-attempt-policy-production-decision-pack-2026-09-03.md) | production |
| `OD-V1-17` | **Dial-token reuse semantics.** Task mang đúng **một** `dial_token` scalar, nhưng policy cần ≥2 customer dial cộng technical retry. Năm tài liệu ghi “one-use/attempt”. Không có endpoint re-issue/refresh trong bất kỳ contract nào. Phương án: (a) `dial_tokens[]` per-attempt, (b) reissue endpoint, (c) token bundle, (d) reusable token có TTL/risk control ghi rõ. | Sales/Security/Telephony | ⏳ **phụ thuộc phương án B — chờ Sếp trả lời mục `B2` phiếu Sếp 25/09** *(sửa 25/09 theo chốt chief: dòng này từng ghi đóng ngày 2026-09-05; phương án B (`W-0311`) đổi ranh giới số điện thoại/token và chưa có chữ ký Sếp)*. Nội dung M8 đã ký, đang thi hành: chọn phương án (d): **token dùng lại được**, gắn cứng vào `task_id`, TTL = cửa sổ xác nhận + 60s, số lần resolve tối đa = `max_customer_attempts` + trần technical retry, mỗi lần resolve gắn một `attempt_id` khác nhau. **Sửa `2026-09-07` (`W-0208`): vế TTL của dòng này chưa thi hành được.** Ba guard đang chạy (intake `TaskIntakeService.ContactRejectionReason`, persistence `PersistenceInvariantValidator.ValidateTask`, dispatch `PostgresTelephonyDispatchStore.LoadAsync`) ép `dial_token_expires_at` **bằng đúng** confirmation-window end; `+60s` bị persistence từ chối sau khi qua intake. Đã pin bằng `IT-INTAKE-DB-03`. Phần token dùng lại/binding/trần resolve **đã** implement (`W-0199`); **Vế TTL chốt lại `2026-09-09` (`W-0246`), owner ký: `dial_token_expires_at` = ĐÚNG confirmation-window end, thay thế `+60s`.** Không tầng nào phải sửa và M3 không phải đổi gì — equality vốn đã là thứ ba guard nhận. Lý do giữ được: token hết hạn đúng cuối cửa sổ nghĩa là không quyền quay số nào sống lâu hơn cửa sổ gọi, còn cuộc đang gọi vẫn an toàn nhờ ràng buộc riêng `DialTokenExpiresAt <= lease.Deadline` ở dispatch. `IT-INTAKE-DB-03` nay ghim một hợp đồng đã ký thay vì một hiện trạng. Trần số lần resolve thay cho “one-use”: token rò rỉ vẫn không quay số được quá số lần chính sách cho phép. Loại (b)/(c) vì bắt Sales dựng endpoint mới — thứ duy nhất có thể hỏng ngay giữa lúc đang gọi | Chọn phương án + issue/resolve/reissue contract + TTL/replay/audit tests | real call |
| `OD-V1-18` | **Vị trí resolve `dial_token→E.164`.** `specs/api/04-sim-adapter-contract.md` nói adapter **không** nhận số; `P2-4` đặt resolver trong IVR. Gateway GSM/SIP thương mại quay số E.164. Trust boundary chưa được định nghĩa ở đâu. | Security + Telephony vendor | ⏳ **phụ thuộc phương án B — chờ Sếp trả lời mục `B2` phiếu Sếp 25/09** *(sửa 25/09 theo chốt chief: dòng này từng ghi đóng ngày 2026-09-05; vế “không ghi DB” đã bị phương án B thay ngày 17/09, xem bên dưới, mà chưa có chữ ký Sếp)*. Nội dung M8 đã ký: resolver nằm **trong IVR**, bên trong biên adapter telephony. Số E.164 tồn tại **chỉ trong bộ nhớ tiến trình** cho đúng một lần quay số: không ghi DB, không vào log, không vào evidence, không vào callback payload. **Sửa `2026-09-17` (`T4` của [bản vướng mắc `17/09`](../../plan/ivr-orther/vuong-mac-va-quyet-dinh-2026-09-17.md); ghi `18/09` ở `W-0316`): vế *"không ghi DB"* bị thay** bởi phương án B (`W-0311`). Module 3 gửi thẳng `phone_e164`, và IVR **lưu** nó trong `ivr_confirmation_tasks`, vì cửa sổ xác nhận dài `5–15` phút và lần gọi thứ hai phải sống qua một lần khởi động lại worker. Ba vế *"không vào log, không vào evidence, không vào callback payload"* **giữ nguyên**. Số bị xoá khi khách yêu cầu (`W-0314`); rủi ro giữ số dạng đọc được là rủi ro `1` ở `S2`. Loại phương án để gateway tự resolve vì nó đẩy toàn bộ ánh xạ token→số sang nhà cung cấp, tức là giao họ cả danh bạ khách. **Nợ tài liệu**: `specs/api/04-sim-adapter-contract.md` đang nói ngược, phải sửa ở GĐ 2 | Sơ đồ trust boundary đã duyệt + threat model + vendor capability statement | LAB_REAL_SIM |
| `OD-V1-19` | **TTS/speech synthesis provider.** Không prompt nào implement audio thật; `P8-1` gọi `play` mà không có nguồn audio. Chọn bộ đọc kéo theo PDPA (nội dung đơn có rời mạng không), cost và pronunciation acceptance. | Product + Infra + Privacy/Legal | ✅ `CLOSED` 2026-09-17 (`S4`, owner; nhắc lại 2026-09-18) — **VieNeu-TTS tự host là bộ đọc duy nhất**, cả production lẫn lab (`W-0122`, VieNeu-TTS v3 Turbo ONNX int8). Không vendor TTS đám mây lúc chạy: sidecar chạy chung pod với worker và chỉ nghe loopback, nên nội dung đơn không rời hệ thống và bài toán DPA/data-residency của vendor không phát sinh. Phần cố định của kịch bản do VieNeu render sẵn (`12` đoạn, ba giọng `OD-VOICE-06`); món, tổng tiền và nơi giao do sidecar tổng hợp lúc gọi. Lời chào không có tên khách (“Xin chào Quý khách”, `W-0104`). **Đính chính 25/09 (`C23`; kế hoạch khắc phục 25/09, `Q-02` phương án 2):** luật quá độ Tech Lead 24/09 thắng `S4` cho production theo thứ tự thắng của chief ([danh sách chief 25/09](../../plan/toan-viec-can-lam-m8-2026-09-25.md), mục `V6-16`). Production **không sinh giọng lúc gọi**: lời thoại là audio dựng sẵn từ template đã duyệt; phần động (nếu có) ghép từ clip VieNeu render trước, offline. Vẫn là “chỉ VieNeu”, không có giọng người — quyết định “chỉ VieNeu” của owner không đổi. Phạm vi phần động Tech Lead đang chốt. Câu “món, tổng tiền và nơi giao do sidecar tổng hợp lúc gọi” ở trên vì vậy không còn là thiết kế production; code hiện vẫn tổng hợp phần động lúc gọi (production chưa bật: `deploy/helm/ivr/values-prod.yaml` đặt `tts.enabled: false`), đổi theo luật này là việc `N1`, chưa làm | `W-0122`: nguồn và model ghim trong `deploy/tts/models/MODELS.lock`, shim loopback, ba giọng đã nghiệm thu. Trước production còn: quyền dùng model (`legal_gate`), kho bản cài nội bộ, rủi ro lỗ hổng image (`S2` rủi ro `3`), đo trên máy thật. *Sửa 25/09: mục thứ ba trước ghi “`16` CVE của base image” — con số của nền Debian cũ quét bằng DB 28/08; cùng image quét DB 18/09 ra `57`, vá còn `44 HIGH` (`W-0317`); nền đã đổi sang Chainguard (`W-0323`), và image VieNeu cuối quét `0` CVE lúc quét ngày 22/09 (`W-0340`, `W-0343`)* | production |
| `OD-V1-20` | **Production RBAC cho runtime-gate controls.** Bộ permission `DF-01` (LOCKED, 7 quyền) không có quyền nào cho phép sửa `labDestinationAllowlist` hoặc `globalDialKillSwitch`. Cần permission mới + four-eyes. | Security/Platform + Release owner | ✅ `CLOSED` 2026-09-05 — thêm `IVR_RUNTIME_GATE_ADMIN` ở tầng danger, bất đối xứng theo `W-0068`: **bật** kill switch cần một người, không cần duyệt (chiều giảm rủi ro); **tắt** kill switch hoặc mở rộng `labDestinationAllowlist` cần bốn mắt — người đề xuất và người duyệt khác nhau, ghi bảng phê duyệt append-only. **Residual**: vế bốn mắt cần người thứ hai, cùng bài toán với `OD-V1-11` | Approved permission set + four-eyes policy + negative authz tests | LAB_REAL_SIM |
| `OD-V1-21` | **GitLab platform provisioning.** ~~TV1-12 khóa GitLab CI nhưng remote duy nhất hiện tại là GitHub.~~ **Sửa `2026-08-27`: vế này đã sai từ W-0011** — GitLab project tồn tại và chính là `origin`; runner `#55115499`, Container Registry, protected branch và hosted MR pipeline đều đã PASS trong evidence W-0011. Cái hỏng suốt từ đó là **lối đẩy code**: `remote.origin.pushurl` trỏ GitHub nên GitLab không nhận commit mới (`W-0121` sửa, GitLab đang thiếu 3 commit lúc phát hiện). Cần GitLab project/mirror, Runner, Container Registry, protected branch, MR approvals, “Pipelines must succeed”, masked/protected variables. | Platform/Infra | ✅ `CLOSED` 2026-09-10 — **owner tuyên bố quorum là chính mình.** Không có independent approver trong tổ chức này. **Đóng cái gì và KHÔNG đóng cái gì:** đóng **quyết định** về cấu hình GitLab. **Không** tạo ra được bằng chứng four-eyes: chứng minh một merge bị chặn tới khi người khác duyệt cần **Premium/Ultimate cộng một reviewer thứ hai**, và điều đó không phụ thuộc chữ ký của ai. Ghi nhận là **giới hạn được chấp nhận**, không phải điều kiện đã thoả. Nguyên văn trạng thái cũ: ⏳ `SIGNED_EXCEPT_INDEPENDENT_APPROVAL` 2026-09-05 — owner ký phần cấu hình: project/runner/registry/protected branch/hosted MR pipeline đều đã PASS trong evidence W-0011. Còn đúng một vế **không ký được bằng chữ ký**: chứng minh một lần merge bị chặn cho tới khi có approval của người khác cần Premium/Ultimate **và** một reviewer thứ hai. Dự án hiện có một người | GitLab project URL + remote verification + runner identity + hosted MR pipeline + protected-branch export + registry push/pull proof + **một MR chứng minh blocked-trước/merge-sau** | P0-2 hosted evidence |

## P2.1 — mở và đóng bởi W-0204 (chốt contract, 2026-09-06)

Hai mục thuộc P2.1 mà **chưa dòng nào trong sổ này chạm tới** trước lượt W-0204. Chúng không phải
câu hỏi mới; chúng là hai chỗ trống bị bỏ sót vì nằm giữa hai tài liệu — một cái ở `api-versioning`,
một cái chỉ tồn tại trong gói M8-08.

| ID | Decision/data | Owner | Current | Closure evidence | Gate |
| --- | --- | --- | --- | --- | --- |
| `OD-V1-22` | **Compatibility window / grace period.** `docs/api-versioning.md` ghi "candidate default: ít nhất 90 ngày" — một **đề xuất kỹ thuật**, không phải cam kết với consumer. P2.1 yêu cầu chốt, và M3 khó ký một cửa sổ tương thích mà phía IVR chưa cam kết | Owner (IVR) | ✅ `CLOSED` 2026-09-06 — owner ký **≥ 90 ngày**, đồng hồ bắt đầu chạy khi successor **vừa được duyệt vừa có mặt ở non-prod** (hai điều kiện, không phải một). Rút ngắn chỉ khi có sự cố bảo mật/riêng tư, kèm phê duyệt owner + security, thông báo consumer và quyết định rollback — giữ nguyên ngoại lệ đã viết. Con số 90 được nhận nguyên vì nó bảo thủ và đã nằm sẵn trong chính sách; đổi nó lúc này chỉ tạo ra một số chưa ai đo | Chính sách đã siết từ "candidate" sang đã ký trong `docs/api-versioning.md`; `Sunset` cụ thể vẫn phải duyệt riêng trước khi phát header | real integration |
| `OD-V1-23` | **Opt-out boundary.** Không tồn tại dòng `OD-V1` nào chạm tới opt-out trước lượt này; câu hỏi chỉ sống ở `OQ-OPTOUT-01` và gói M8-08. Hệ hiện tại có hằng số `2/3` (suy opt-out từ nhiều lần `Rejected`) **không có authority nào chống lưng** | Product + CRM/M3 + Legal/Privacy | ✅ `CLOSED` 2026-09-10 — **owner tuyên bố quorum là chính mình**, vị trí explicit-only V1 thành chốt. **Đóng cái gì và KHÔNG đóng cái gì:** đóng **ranh giới** opt-out. **Không** lấp được khoảng trống nêu ngay bên dưới — V1 hiện **không có tín hiệu opt-out tường minh nào** (DTMF-0 là phím hủy đơn, phím 9 ngoài scope và bị `TargetV1SpeechPolicy.ValidateTemplate` từ chối). Hệ quả thực tế của "explicit-only" ở V1 là **không có opt-out**, và thêm một tín hiệu là một OD mới, không phải một dòng code. Hằng số `2/3` vẫn phải xử lý như khoảng trống. Nguyên văn trạng thái cũ: ⏳ `OWNER_POSITION_SIGNED / QUORUM_PENDING` 2026-09-06 — owner ký **explicit-only V1**: chỉ coi là opt-out khi khách phát tín hiệu tường minh (DTMF-0 / handoff). **Không** suy ra từ số lần `Rejected`; muốn suy diễn thì phải ký một contract V2 riêng. **Sửa `2026-09-07` (`W-0210`): hai ví dụ tín hiệu trong ngoặc đều sai, nguyên tắc "explicit-only" thì giữ.** `DTMF-0` là **phím hủy đơn** — lời thoại khóa cứng nói *"bấm phím 0 để hủy"* (`TargetV1SpeechPolicy`), và M8-08 §4.2 — chính gói được dòng này dẫn làm closure evidence — ghi *"DTMF 1 là xác nhận đơn, DTMF 0 là yêu cầu huỷ đơn. **Không tái dùng hai phím này cho opt-out**"*. `handoff`/phím 9 thì **ngoài scope** (`specs/01-context-and-scope.md:34`) và `TargetV1SpeechPolicy.ValidateTemplate` **từ chối** mọi template có chữ "phím 9". Vậy V1 hiện **không có tín hiệu opt-out tường minh nào**; thêm một cái cần wording/script + signal source + proof + Legal/Privacy ký riêng (M8-08 §4.3). Đọc phím hủy thành lệnh cấm liên hệ là lấy consent khách chưa từng cho, từ một câu khách chưa từng nghe. Đã ghim bằng `UT-OPTOUT-DTMF0-05`. Hằng số `2/3` vì vậy là hành vi không có cơ sở và phải được xử lý như một khoảng trống, không phải như một quy tắc. **Đây mới là vị trí của owner, chưa phải quorum**: Legal/Privacy và CRM/M3 vẫn phải đồng ký trước khi có bất kỳ code opt-out nào | `OPT-01..11` đã ký đủ quorum + schema/producer/consumer SHA + [M8-08](../../plan/ivr-orther/m8-08-opt-out-suppression-decision-pack-2026-09-03.md) | **mọi code opt-out và feedback E2E** |

## Mở và đóng ngày 2026-09-17 — bản vướng mắc (ghi `18/09` ở `W-0316`)

Một giới hạn của bản đầu được chấp nhận có chủ ý. Ghi ở đây để không ai đọc `m8-15` rồi tưởng việc
dựng registry ngoài vẫn đang chờ.

| ID | Decision/data | Owner | Current | Closure evidence | Gate |
| --- | --- | --- | --- | --- | --- |
| `OD-V1-24` | **Registry ngoài cho checkpoint của sổ cái dung lượng** (`m8-15`, `W-0160`). Sổ cái metadata (`W-0158`) có chuỗi hash nội bộ, và checkpoint (`W-0159`) bắt được sửa byte hay cắt giữa dòng. Nhưng nếu có ai đưa lại một cặp checkpoint cũ còn hợp lệ, không có dữ kiện độc lập nào cho biết đó là rollback. `m8-15` là hợp đồng cho một registry **nằm ngoài** IVR để chặn chỗ đó: sequence tăng đúng một, compare-and-swap, người giữ tách khỏi người ghi sổ. Chưa chọn provider, chưa ký | Sếp + Toàn | ✅ `CLOSED` 2026-09-17 — **phương án B (`S7`)**: bản đầu **không** mua hay dựng registry ngoài; đây là **giới hạn được chấp nhận** của bản đầu. Hệ quả: rollback sổ cái dung lượng về một checkpoint cũ còn hợp lệ **không bị phát hiện**. **Không sửa** `m8-15`, vì file bị ghim hash ở `deploy/ci/scripts/capacity-registry-decision-pack-validator.mjs:27`; quyết định được ghi ở dòng này. Mở lại thì cần một OD mới | [bản vướng mắc `17/09`](../../plan/ivr-orther/vuong-mac-va-quyet-dinh-2026-09-17.md) §1, dòng `S7` | — *(giới hạn đã nhận)* |

## Explicit non-decisions

- V1 notification is disabled; no notification template/event is required. Bất biến này được enforce ở `P0-4` (`v1NotificationEnabled=false` immutable guard), fail-gate 3 trong `specs/testing/08-acceptance-criteria.md`, và `IT-FAILGATE-*` ở `P5-1` — **không** chỉ dựa vào `P4-5`.
- IVR remains a standalone .NET service; Sales remains Java and owns order truth.
- Current Golden Hour callback remains compatibility-only và không được nhận kết quả 24/7.
- `IMPLEMENTATION_COMPLETE_BEHIND_MOCKS` may be reached while all external rows remain open, but integration/production states must remain blocked.
- Internal API (`specs/api/openapi/ivr-order-confirmation.v1.yaml`) và outbound Sales callback (`order-core-ivr-callback.target-v1.yaml`) là **hai surface riêng biệt**; naming khác nhau là chủ ý và phải map bằng mapper tường minh.

## P1 — mở bởi W-0106 (định tuyến giọng theo vùng miền, 2026-08-22)

| ID | Decision/data | Owner | Current | Closure evidence | Gate |
| --- | --- | --- | --- | --- | --- |
| `OD-VOICE-01` | **Nguồn giọng production.** | Product + Infra + Privacy/Legal | ✅ `CLOSED` 2026-09-17 — VieNeu-TTS tự host (`W-0122`), cùng quyết định với `OD-V1-19`. Lab cũng chỉ dùng VieNeu | `W-0122` provenance + ba giọng Owner duyệt (`OD-VOICE-06`); các gate production liệt kê ở `OD-V1-19` | production |
| `OD-VOICE-02` | **Phân miền theo tỉnh/thành.** Chia thuần theo 34 đơn vị cấp tỉnh (NQ `202/2025/QH15`), không biệt lệ; Tây Nguyên → Trung | Owner + Product | ✅ `CLOSED` 2026-08-22 | Bảng 34→3 miền + `UT-VOICE-REGION-01..03` phủ 34 tỉnh mới và 29 tên cũ | — |
| `OD-VOICE-03` | **Một template.** Giữ đúng 1 script version `v3-test-approved`; biến thể `nghìn`/`ngàn` và `linh`/`lẻ` nằm trong bộ đọc số, không nằm trong template ⇒ `TemplateHash` không đổi | Product + Privacy/Legal | ✅ `CLOSED` 2026-08-22 | `UT-SCRIPT-VI-REGION-09` chứng minh 3 miền cùng một `TemplateHash` | — |
| `OD-VOICE-04` | **Tự host thay vì thuê vendor.** Model tự host phải được phép dùng thương mại: `viXTTS` (CPML phi thương mại) và `F5-TTS` (weights CC-BY-NC) bị loại vì lý do đó | Owner + Product + Legal | ✅ `CLOSED` 2026-09-17 — tự host **VieNeu-TTS v3 Turbo**. Model card khai Apache-2.0 nhưng revision đã ghim chưa có file `LICENSE`. *Sửa 25/09 — chỉ ghi hiện trạng, lock không đổi:* câu cũ ở đây nói `MODELS.lock` giữ `legal_gate=OWNER_DATA_REQUIRED`; hiện lock ghi `legal_gate.status=PASS`, `decision_authority=LEGAL_PRIVACY`, người quyết Nguyễn Quốc Toàn, ngày 2026-09-22 (`docs/evidence/W-0343/legal-approval.json`) — Toàn tự xác nhận thẩm quyền kiêm nhiệm Legal/Privacy, và hồ sơ tự ghi đây không phải rà soát pháp lý độc lập. Bằng chứng nhà phát hành công bố Apache-2.0 đúng revision nằm ở `W-0341`. Chief yêu cầu, nếu không có văn bản ủy quyền của Sếp, hạ `legal_gate` về “Toàn đã rà — chờ Sếp ký” (phiếu Sếp 25/09 mục `B2`); quyết định hạ (`Q-17` của kế hoạch khắc phục 25/09) chưa chốt, nên lock chưa hạ | `deploy/tts/models/MODELS.lock` + `deploy/tts/THIRD_PARTY_NOTICES.md`; quyết định pháp lý có `decision_authority`, người ký và ngày | production |
| `OD-VOICE-05` | **Ba giọng vùng miền.** | Owner | ✅ `CLOSED` 2026-08-28 — chốt bằng `OD-VOICE-06`: Owner nghe đủ `11` giọng VieNeu qua MicroSIP 8 kHz và chọn **Ngọc Linh** (Bắc), **Ngọc Trân** (Trung), **Mỹ Duyên** (Nam) | `docs/evidence/W-0122/voice-acceptance-manifest.json` (Owner ký) qua `tts-voice-acceptance-gate.mjs` | LAB |

> Ba giọng VieNeu và `12` đoạn cố định đã render là tài sản lab cho tới khi các gate production ở
> `OD-V1-19` đóng. Nghe duyệt `12` đoạn (cả chỗ nối với phần tổng hợp lúc gọi) vẫn là việc của
> người duyệt được chỉ định.
>
> *Đính chính 25/09 (`C23`):* ở production, theo luật quá độ Tech Lead 24/09, không có phần tổng hợp lúc
> gọi; chỗ nối cần nghe duyệt là chỗ nối với clip VieNeu render trước, nếu có phần động (xem `OD-V1-19`).
